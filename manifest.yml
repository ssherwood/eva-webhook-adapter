applications:
- name: eva-webhook-adapter
  buildpacks:
  - java_buildpack_offline
  env:
    WEBHOOK_TARGET_URL: http://localhost:8080/v1/webhooks/incoming/webex-text
    WEBHOOK_TARGET_FORMAT: webex-text